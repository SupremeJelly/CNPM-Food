# name: CI Pipeline - Full Testing & Build

# on:
#   push:
#     branches:
#       - src
#       - main
#   pull_request:
#     branches:
#       - src
#       - main

# jobs:
#   # JOB 1: Test All Backend Services
#   test-backend:
#     name: Test Backend Services
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false  # Continue testing other services even if one fails
#       matrix:
#         service: [user-service, restaurant-service, order-service, payment-service, api-gateway]
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up JDK 21
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '21'
#           cache: 'maven'

#       - name: Cache Maven dependencies
#         uses: actions/cache@v4
#         with:
#           path: ~/.m2/repository
#           key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
#           restore-keys: |
#             ${{ runner.os }}-maven-

#       - name: Run Tests for ${{ matrix.service }}
#         working-directory: ./${{ matrix.service }}
#         run: |
#           chmod +x mvnw
#           ./mvnw clean test -B
#         continue-on-error: false

#       - name: Generate Test Report for ${{ matrix.service }}
#         if: always()
#         uses: dorny/test-reporter@v1
#         with:
#           name: Test Results - ${{ matrix.service }}
#           path: ${{ matrix.service }}/target/surefire-reports/*.xml
#           reporter: java-junit
#           fail-on-error: false

#       - name: Upload Test Results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: test-results-${{ matrix.service }}
#           path: ${{ matrix.service }}/target/surefire-reports/
#           retention-days: 7

#   # JOB 2: Test Frontend
#   test-frontend:
#     name: Test Frontend
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json

#       - name: Cache Node modules
#         uses: actions/cache@v4
#         with:
#           path: frontend/node_modules
#           key: ${{ runner.os }}-node-${{ hashFiles('frontend/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-node-

#       - name: Install dependencies
#         working-directory: ./frontend
#         run: |
#           rm -rf node_modules package-lock.json
#           npm install

#       - name: Run Frontend Linting
#         working-directory: ./frontend
#         run: npm run lint || echo "Linting completed with warnings"
#         continue-on-error: true

#       - name: Run Frontend Tests
#         working-directory: ./frontend
#         run: |
#           npm run test -- --watch=false --browsers=ChromeHeadless --code-coverage
#         continue-on-error: true

#       - name: Upload Coverage Reports
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: frontend-coverage
#           path: frontend/coverage/
#           retention-days: 7

#   # JOB 3: Build Backend Services
#   build-backend:
#     name: Build Backend Services
#     needs: test-backend
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         service: [user-service, restaurant-service, order-service, payment-service, api-gateway]
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up JDK 21
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '21'
#           cache: 'maven'

#       - name: Set up JDK 21
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '21'
#           cache: 'maven'

#       - name: Cache Maven dependencies
#         uses: actions/cache@v4
#         with:
#           path: ~/.m2/repository
#           key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
#           restore-keys: |
#             ${{ runner.os }}-maven-

#       - name: Build ${{ matrix.service }}
#         working-directory: ./${{ matrix.service }}
#         run: |
#           chmod +x mvnw
#           ./mvnw clean package -DskipTests -B

#       - name: Upload Build Artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: ${{ matrix.service }}-jar
#           path: ${{ matrix.service }}/target/*.jar
#           retention-days: 7

#   # JOB 4: Build Frontend (runs after frontend tests pass)
#   build-frontend:
#     name: Build Frontend
#     needs: test-frontend
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies
#         working-directory: ./frontend
#         run: |
#           rm -rf node_modules package-lock.json
#           npm install

#       - name: Build Frontend for Production
#         working-directory: ./frontend
#         run: npm run build -- --configuration production

#       - name: Upload Build Artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: frontend-dist
#           path: frontend/dist/
#           retention-days: 7

#   # JOB 5: Code Quality & Security Checks
#   code-quality:
#     name: Code Quality Analysis
#     runs-on: ubuntu-latest
#     needs: [test-backend, test-frontend]
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Full history for SonarQube

#       - name: Set up JDK 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '17'

#       - name: Check for Security Vulnerabilities (Backend)
#         working-directory: ./user-service
#         run: |
#           chmod +x mvnw
#           ./mvnw dependency:tree
#           ./mvnw org.owasp:dependency-check-maven:check || echo "Security scan completed"
#         continue-on-error: true

#       - name: Check for Security Vulnerabilities (Frontend)
#         working-directory: ./frontend
#         run: |
#           npm audit --production || echo "npm audit completed"
#         continue-on-error: true

#   # JOB 6: Integration Test Summary
#   test-summary:
#     name: Test Summary Report
#     runs-on: ubuntu-latest
#     needs: [test-backend, test-frontend, build-backend, build-frontend]
#     if: always()
    
#     steps:
#       - name: Download all test results
#         uses: actions/download-artifact@v4
#         with:
#           path: test-results

#       - name: Generate Summary
#         run: |
#           echo "## Test & Build Summary" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### Completed Jobs:" >> $GITHUB_STEP_SUMMARY
#           echo "- Backend Tests: ${{ needs.test-backend.result }}" >> $GITHUB_STEP_SUMMARY
#           echo "- Frontend Tests: ${{ needs.test-frontend.result }}" >> $GITHUB_STEP_SUMMARY
#           echo "- Backend Build: ${{ needs.build-backend.result }}" >> $GITHUB_STEP_SUMMARY
#           echo "- Frontend Build: ${{ needs.build-frontend.result }}" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### Artifacts Generated:" >> $GITHUB_STEP_SUMMARY
#           echo "- Backend JARs: " >> $GITHUB_STEP_SUMMARY
#           echo "- Frontend Dist: " >> $GITHUB_STEP_SUMMARY
#           echo "- Test Reports: " >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "###  Ready for Deployment!" >> $GITHUB_STEP_SUMMARY

#       - name: Comment PR with Results
#         if: github.event_name == 'pull_request'
#         uses: actions/github-script@v7
#         with:
#           script: |
#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: '##  CI Pipeline Results\n\n' +
#                     ' All tests and builds completed!\n\n' +
#                     '**Backend Tests:** ${{ needs.test-backend.result }}\n' +
#                     '**Frontend Tests:** ${{ needs.test-frontend.result }}\n' +
#                     '**Backend Build:** ${{ needs.build-backend.result }}\n' +
#                     '**Frontend Build:** ${{ needs.build-frontend.result }}\n\n' +
#                     'Ready to merge! '
#             })
